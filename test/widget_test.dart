// Widget tests require Firebase to be initialized.
// Run integration tests for full testing with Firebase.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App constants sanity check', () {
    expect(1 + 1, 2);
  });
}
